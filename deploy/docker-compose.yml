services:
  unity-webgl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: velora-client-build
    restart: always
    ports:
      - "8090:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
