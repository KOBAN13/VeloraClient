services:
  unity-webgl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: velora-client-build
    restart: always
    ports:
      - "8090:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
